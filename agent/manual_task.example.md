# Task

Goal:
- Improve the homepage hero and first fold so it looks more premium and readable.

Non-goals:
- Do not change backend APIs.
- Do not add new third-party dependencies unless absolutely necessary.

Acceptance tests:
- `npm run lint` passes.
- `npm run build` passes.
- The homepage renders without console errors.
- CTA is visually obvious and spacing looks balanced on desktop.

Architecture constraints:
- Prefer changes inside existing frontend components and styles.
- Keep the routing structure intact.

Stop if:
- A backend schema or auth change is required.
- More than 5 files must change.
